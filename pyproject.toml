[project]
name = "rlm"
version = "0.0.1-prerelease.1"
dependencies = [
    "torch>=2.2.0",
    "pandas",
    "numpy",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
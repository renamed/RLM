[project]
name = "rlm"
version = "0.0.1-prerelease.4"
dependencies = [
    "torch>=2.11.0,<3.0.0",
    "pandas>=3.0.1,<4.0.0",    
    "numpy>=2.4.0,<3.0.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"